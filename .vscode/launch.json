{
  "version": "0.2.0",
  "configurations": [{
    "name": "winBCF",
    "type": "python",
    "request": "launch",
    "program": "${workspaceFolder}\\winBCF.pyw",
    "console": "integratedTerminal",
    "cwd": "${workspaceFolder}"
  }]
}